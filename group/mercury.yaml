name: mercury
label:
  en_US: Mercury
  zh_Hans: Mercury
icon: _assets/icon.svg
description:
  en_US: Mercury Sync Endpoints

settings:
  - name: webhook_secret
    type: secret-input
    required: false
    label:
      en_US: Webhook Secret
    placeholder:
      en_US: Optional secret for validation

  - name: app
    type: app-selector
    required: true
    scope: all
    label:
      en_US: Target Dify Application
    help:
      en_US: Select the application or workflow to trigger

endpoints:
  - endpoints/webhook.yaml
