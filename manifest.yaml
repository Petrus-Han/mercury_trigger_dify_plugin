version: 0.3.0
type: plugin
author: petrus
name: mercury_trigger
created_at: 2026-01-12T00:00:00Z
label:
  en_US: Mercury Transaction Trigger
  zh_Hans: Mercury 交易触发器
icon: icon.svg
description:
  en_US: Mercury Banking trigger plugin for receiving transaction events. Works with external poller since Mercury doesn't support native webhooks.
  zh_Hans: Mercury 银行触发器插件，用于接收交易事件。由于 Mercury 不支持原生 webhook，需配合外部轮询器使用。

meta:
  version: 0.3.0
  minimum_dify_version: 1.10.0
  arch:
    - amd64
    - arm64
  runner:
    language: python
    version: "3.12"
    entrypoint: main

resource:
  memory: 1048576
  permission:
    tool:
      enabled: true
    storage:
      enabled: true
      size: 1048576

plugins:
  triggers:
    - provider/mercury.yaml

tags:
  - finance
  - banking
  - automation
